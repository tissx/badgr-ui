import { MozzListenerDirective } from './mozz-listener.directive';

describe('MozzListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MozzListenerDirective();
    expect(directive).toBeTruthy();
  });
});
